---
description: Set up a new project from scratch — interview, stack, design, scaffold, harness, backlog. Runs the full Project Setup Kit chain.
---

Invoke the `setup-project` skill and follow it in full.

If `SETUP_PROGRESS.md` already exists, you are **resuming** — read it and continue at the step it names. Do not restart the chain.

$ARGUMENTS
